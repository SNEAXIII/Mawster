{$DOMAIN:localhost}

{$DOMAIN} {
    handle_path /api/back/* {
        reverse_proxy api:8000
    }

    handle_path /static/* {
        reverse_proxy api:8000
    }

    handle {
        reverse_proxy front:3000
    }
}
