Lance les serveurs en mode test (MariaDB-test 3307 + API 8001 + Frontend 3001). N'affecte pas le mode dev.

Use `mcp__server-runner__start_test` to start all test servers, then report the ports and PIDs.
