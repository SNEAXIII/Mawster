Lance tous les tests Cypress (les serveurs doivent déjà être en mode test).

Use `mcp__cypress-runner__run_all_tests` and report the results.

## Note
Si les serveurs ne sont pas démarrés, utilise `/test-e2e` à la place.
