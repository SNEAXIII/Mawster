Relance uniquement les tests pytest qui ont échoué précédemment.

## Avant de lancer les tests

Toujours exécuter en premier : `rm -rf api/temp/*` (vide le dossier des bases SQLite de test).

Use `mcp__pytest-runner__run_failing_tests` and report the results.
