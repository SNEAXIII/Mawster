---
name: server-stop
description: Use when stopping all running servers — stops both dev and test environments.
---

# Server Stop

Arrête tous les serveurs (dev + test).

```
mcp__server-runner__stop()
```
