---
name: server-status
description: Use when checking if dev or test servers are running — shows ports, PIDs, and uptime.
---

# Server Status

Retourne l'état des serveurs dev et test.

```
mcp__server-runner__status()
```

Dev : API `8000`, Frontend `3000`. Test : API `8001`, Frontend `3001`.
