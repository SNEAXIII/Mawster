---
name: server-dev
description: Use when starting the dev server — launches MariaDB, FastAPI (8000), and Next.js (3000) in dev mode.
---

# Server Dev

Lance l'environnement de dev complet.

```
mcp__server-runner__start_dev()
```

Ports : API `8000`, Frontend `3000`, phpMyAdmin `8080`.
