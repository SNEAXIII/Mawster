networks:
  internal:
    driver: bridge

services:
  mariadb:
    image: mariadb:11.4
    container_name: mariadb
    restart: unless-stopped
    networks:
      - internal
    env_file: db.env
    volumes:
      - data_db:/var/lib/mysql
    healthcheck:
      test: ['CMD', 'healthcheck.sh', '--connect', '--innodb_initialized']
      interval: 10s
      timeout: 5s
      retries: 5
    logging:
      driver: 'json-file'
      options:
        max-size: '10m'
        max-file: '5'

  backup:
    build: ./backup
    container_name: backup
    restart: unless-stopped
    networks:
      - internal
    depends_on:
      mariadb:
        condition: service_healthy
    env_file:
      - db.env
    volumes:
      - ./backups:/backups
      - ./backup/rclone.conf:/root/.config/rclone/rclone.conf:ro
    environment:
      - TZ=Europe/Paris
    logging:
      driver: "json-file"
      options:
        max-size: "5m"
        max-file: "3"

  api:
    image: sneaxiii/mawster-api:20260420085239
    # build:
      # context: ./api
      # dockerfile: api.Dockerfile
    container_name: api
    restart: unless-stopped
    networks:
      - internal
    depends_on:
      mariadb:
        condition: service_healthy
    env_file:
      - api.env
    command: ['sh', 'run.sh']
    # Mount logs directory on the host for persistence and inspection
    volumes:
      - ./api/logs:/app/api/logs
    # Explicitly set production mode so backend behaves accordingly
    environment:
      - MODE=prod
      - CONTAINER=1
    # Docker-level log driver: rotate JSON logs at container level (also captured by app.log/audit.log)
    logging:
      driver: 'json-file'
      options:
        max-size: '10m'
        max-file: '5'

  front:
    image: sneaxiii/mawster-front:20260420085240
    # build:
      # context: ./front
      # dockerfile: front.Dockerfile
    container_name: front
    restart: unless-stopped
    networks:
      - internal
    depends_on:
      - api
    env_file:
      - front.env
    command: ['sh', 'run.sh']
    logging:
      driver: 'json-file'
      options:
        max-size: '10m'
        max-file: '5'

  static:
    image: sneaxiii/mawster-static:latest
    container_name: static
    restart: unless-stopped
    networks:
      - internal
    environment:
      - API_HOST=api:8000
    depends_on:
      - api
    logging:
      driver: 'json-file'
      options:
        max-size: '10m'
        max-file: '5'

  caddy:
    image: caddy:2.10
    container_name: caddy
    restart: unless-stopped
    ports:
      - '80:80'
      - '443:443'
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - ./caddy/data:/data
      - ./caddy/config:/config
    networks:
      - internal
    depends_on:
      - front
      - api
    logging:
      driver: 'json-file'
      options:
        max-size: '10m'
        max-file: '5'

volumes:
  data_db:
